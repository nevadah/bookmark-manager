DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/bookmark_manager"

# AI tag suggestions (optional — omit to disable server-side AI tagging)
# AI_PROVIDER=anthropic         # anthropic | openai | azure-openai | openrouter
# AI_API_KEY=your-api-key
# AI_AZURE_ENDPOINT=            # required if AI_PROVIDER=azure-openai
# AI_AZURE_DEPLOYMENT=          # required if AI_PROVIDER=azure-openai
# AI_OPENROUTER_MODEL=          # required if AI_PROVIDER=openrouter
